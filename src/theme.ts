const mode = "dark"

const color = {}

const typography = {}

const spacing = {}

const size = {}

const borderRadius = {}

const zIndex = {}

const config = {}

const theme = { mode, color, typography, spacing, size, borderRadius, zIndex, config }

export default theme
